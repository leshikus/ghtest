name: Build
on:
  workflow_dispatch:
    inputs:
      cache:
        type: boolean
        default: true

  push:
    paths-ignore:
      - 'docs/**'

    inputs:
      cache:
        type: boolean
        default: true

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{ inputs.cache }}

