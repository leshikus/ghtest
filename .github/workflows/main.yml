name: Benchmarking
on:
  push

jobs:
  test1:
    if: false
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo test1

  test2:
    needs: test1
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo test2
