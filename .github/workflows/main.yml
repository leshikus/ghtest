name: Benchmarking
on:
  push

jobs:
  test1:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "${{ toJSON(job) }}"

  test2:
    needs: test1
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo test2
